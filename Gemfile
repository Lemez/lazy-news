source 'https://rubygems.org'
ruby "2.0.0"

# Bundle edge Rails instead: gem 'rails', github: 'rails/rails'
gem 'rails', '4.0.0'

# Use sqlite3 as the database for Active Record
group :development, :test do
    gem 'sqlite3'
end

group :production do
    gem 'pg'
end

# Use SCSS for stylesheets
gem 'sass-rails', '~> 4.0.0'

# Use Uglifier as compressor for JavaScript assets
gem 'uglifier', '>= 1.3.0'

# Use CoffeeScript for .js.coffee assets and views
gem 'coffee-rails', '~> 4.0.0'

# See https://github.com/sstephenson/execjs#readme for more supported runtimes
# gem 'therubyracer', platforms: :ruby

# Use jquery as the JavaScript library
gem 'jquery-rails'

# Turbolinks makes following links in your web application faster. Read more: https://github.com/rails/turbolinks
gem 'turbolinks'

# Build JSON APIs with ease. Read more: https://github.com/rails/jbuilder
gem 'jbuilder', '~> 1.2'

group :doc do
  # bundle exec rake doc:rails generates the API under doc/api.
  gem 'sdoc', require: false
end

# Use ActiveModel has_secure_password
# gem 'bcrypt-ruby', '~> 3.0.0'

# Use unicorn as the app server
# gem 'unicorn'

# Use Capistrano for deployment
# gem 'capistrano', group: :development

# Use debugger
# gem 'debugger', group: [:development, :test]

# gem for grabbing data from other sites
gem 'httparty'

# parsing data turns it into html
gem 'nokogiri'

# skeleton for RSS
gem "feedjira"

# schedule cron tasks for rake
gem 'whenever', :require => false

# gem for wordclouds
gem "jqcloud-rails"

# gem for removing HTML symbols
gem 'sanitize', :require => 'sanitize'

# gem for adding images
gem 'paperclip'

# security for Rails 4 (paperclip related) attr accessible
gem 'protected_attributes'

gem 'awesome_print'

gem "selenium-webdriver", "~> 2.41.0"

gem "phantomjs", "~> 1.9.2.0"

group :development do
  gem "better_errors"
end

gem 'rails_12factor', group: :production





