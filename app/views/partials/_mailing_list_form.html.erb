<%= form_for(@user) do |f| %> 
	<div class="field">
		<%= f.label :email %> <%= f.text_field :email %>
	</div>
	<div class="actions">
		<%= f.submit "Sign up" %> 
	</div>
<% end %>

<% @flash.each do |key, value| %>
<div class="flash <%= key %>"><%= value %></div>
<% end %>