<article id='SIGNUP'>
	<%# render "partials/source_sidebar" %>
	<section class="container">
		<div class='mailer'>
			 <% flash.each do |key, value| %>
		        <div class="alert alert-<%= key %>"><%= value %></div>
		      <% end %>
		<%= render 'partials/mailing_list_form' %>
		<button onclick="location.href='/mailer/preview_welcome';">Click here to see last week's mailout</button>
		</div>
	</section>
</article>
